import { createTheme } from '../../../theme';

export const ErrorTooltipTheme = createTheme({
  id: 'ErrorTooltipTheme',
  definition: {
    backgroundColor: '#870000',
    textColor: 'white',
    titleFontSize: 14,
    titleFontWeight: 600,
    messageFontSize: 12,
    messageFontWeight: 400,
  },
});