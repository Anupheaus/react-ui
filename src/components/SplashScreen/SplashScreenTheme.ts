import { createTheme } from '../../theme';

export const SplashScreenTheme = createTheme({
  id: 'SplashScreenTheme',
  definition: {
    backgroundColor: '#eee',
    textColor: '#000',
  },
});
