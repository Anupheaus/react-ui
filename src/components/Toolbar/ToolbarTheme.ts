import { createTheme } from '../../theme';

export const ToolbarTheme = createTheme({
  id: 'ToolbarTheme',
  definition: {
    backgroundColor: 'transparent',
    borderColor: '#eee',
    borderRadius: 4,
  },
});
