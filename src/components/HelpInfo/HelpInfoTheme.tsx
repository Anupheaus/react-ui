import { FiHelpCircle } from 'react-icons/fi';
import { createTheme } from '../../theme';

export const HelpInfoTheme = createTheme({
  id: 'HelpInfoTheme',
  definition: {},
  icons: {
    help: FiHelpCircle,
  },
});
