/** Default minimum width for a window when not specified via props. */
export const DEFAULT_WINDOW_MIN_WIDTH = 200;

/** Default minimum height for a window when not specified via props. */
export const DEFAULT_WINDOW_MIN_HEIGHT = 150;
