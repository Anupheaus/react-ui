import { createTheme } from '../../theme';

export const TooltipTheme = createTheme({
  id: 'TooltipTheme',
  definition: {
    backgroundColor: '#555',
    textColor: '#fff',
    fontSize: 13,
    fontWeight: 400,
  },
});