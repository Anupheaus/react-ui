import { createTheme } from '../../theme';

export const CheckboxTheme = createTheme({
  id: 'CheckboxTheme',
  definition: {
    backgroundColor: 'transparent',
    uncheckedColor: '#ddd',
    checkedColor: '#999',
  },
});
